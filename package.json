{
  "name": "sdgs-news-classifier",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "preview": "next build && wrangler pages dev",
    "deploy": "next build && wrangler pages deploy --commit-dirty=true",
    "lint": "next lint"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  },
  "overrides": {
    "protobufjs": "^7.6.5"
  }
}
