# 把這個檔案複製成 .env.local,並填入你的 Gemini API key
# 取得方式:https://aistudio.google.com/apikey
GEMINI_API_KEY=

# 選用:指定模型。留空即可,程式會自動挑選可用的最新模型,
# 並在模型失效時自動 fallback 到下一個。
# 只有想「鎖定」某個特定模型時才需要填。
# GEMINI_MODEL=gemini-flash-latest
